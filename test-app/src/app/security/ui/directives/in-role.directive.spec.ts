import { InRoleDirective } from './in-role.directive';

describe('InRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new InRoleDirective();
    expect(directive).toBeTruthy();
  });
});
